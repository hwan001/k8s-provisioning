controllerManager:
  tlsSecret:
    create: true
    name: controller-manager
    certManager:
      enabled: true
      issuerName: selfsigned-issuer # 또는 letsencrypt-prod 등
      issuerKind: Issuer            # 또는 ClusterIssuer
      issuerGroup: cert-manager.io

webhookConfig:
  enabled: true
  host: arc.avgmax.team
  port: 443 