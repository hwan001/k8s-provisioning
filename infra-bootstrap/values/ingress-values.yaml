gateways:
  istio-ingressgateway:
    type: LoadBalancer
    ports:
      - port: 80
        name: http
        targetPort: 8080
    # ingressGateway:
    #   enabled: true
    #   servers:
    #     - port:
    #         number: 80
    #         name: http
    #         protocol: HTTP
    #       hosts:
    #         - "*.k8s.avgmax.team" 
    containerPorts:
      http: 8080

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5